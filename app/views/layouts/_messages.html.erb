<% flash.each do |name, message| %>
  <% if message.is_a? String %>
    <p style="color: <%= name == 'success' || name == 'notice' ? 'green' : 'red' %>">
      <%= message %>
    </p>
  <% end %>
<% end %>